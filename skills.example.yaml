skills:
  motion_plan_skill:
    adapter: mock
    mock_type: motion_plan
    description: Phase-1 mock motion planning skill. It validates ABS_JOINT params only.

  qr_detect_skill:
    adapter: mock
    mock_type: generic
    description: Placeholder mock algorithm skill for future Python script adapter.
